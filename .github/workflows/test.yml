name: Test Runner

on:
  workflow_dispatch:

jobs:
  first-test:
    runs-on: self-hosted
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2
      - name: First
        run: pwd
      - name: Do a volume mount
        run: |
          docker run -v ${{ github.workspace }}/RDG_DATA/:/data -e DEBUG=1 -e OUTPATH=/data -e URI=https://heidicon.ub.uni-heidelberg.de/api/v1/plugin/base/oai/oai -e METADATA=lido -e MAX_COUNT=500 ghcr.io/ise-fizkarlsruhe/rdg_harvest_oaipmh:main
